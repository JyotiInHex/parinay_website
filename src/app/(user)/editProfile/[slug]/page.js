import React from "react";
import { WordStaggerFlowTitle } from "@/components/ui/sectionTitle";

const CompleteProfile = () => {
  return (
    <div className="flex flex-col items-center justify-center h-[60vh] ">
      <WordStaggerFlowTitle className="text-4xl lg:text-6xl text-zinc-800 font-porinoi-sans font-semibold">
        Under Development.
      </WordStaggerFlowTitle>
      <WordStaggerFlowTitle className="text-xl lg:text-4xl text-zinc-800 font-porinoi-sans font-semibold">
        We are working hard to bring you this feature soon.
      </WordStaggerFlowTitle>
    </div>
  )
}

export default CompleteProfile